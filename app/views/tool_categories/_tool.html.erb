


            <tr>
                <td><%= tool_list.name %></td>
                <td><%= tool_category.name %></td>
                <td><%= tool_list.tool_numbers.count %></td>
                <td>
                    <div class="btn-group" role="group" aria-label="Basic example">
                        <%= nav_tab "Show", tool_category_tool_list_path(tool_category, tool_list), :method => "get", :class => 'btn btn-sm btn-outline-primary' %>
                        <%= nav_tab "Edit", edit_tool_category_tool_list_path(tool_category, tool_list), :method => "get", :class => 'btn btn-sm btn-outline-secondary' %>
                        <%= nav_tab "Delete", tool_category_tool_list_path(tool_category, tool_list), :method => "delete", :class => 'btn btn-sm btn-outline-danger', form: {data: {confirm: "Are you sure?"}} %>
                    </div>
                </td>
            </tr>