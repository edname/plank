
    <% provide :page_title, "Tools" %>
    <% currently_at "Tools variant show" %>

    <div class="d-flex flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h5"><%= @tool_variant.tool_number %> - <%=@tool.name %></h1>
    </div>

    <div class="card">
        <div class="card-body">
            <h5 class="card-title"><%=@tool.name %></h5>
            <p class="card-text"><%= @tool_variant.tool_number %></p>
            <p class="card-text"><%= @tool_variant.body %></p>
        </div>
    </div>